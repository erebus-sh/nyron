name: Publish Github Release Using Nyron

on:
  push:
    tags:
      - 'nyron-release@*'

jobs:
  publish-release:
    runs-on: ubuntu-latest
    steps:
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Set up environment variables
        run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
      - run: bun install
      - run: bun x @nyron/cli release